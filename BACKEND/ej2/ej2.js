/* Crea un archivo con un objeto con tu nombre y tu edad y después saca por consola, al ejecutarlo con Node,
ambos datos recogiéndolos del objeto. */
const myself = {
    name: "Maria",
    age: 28
}

console.log(myself.name, myself.age)